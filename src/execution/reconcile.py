"""src/execution/reconcile.py — §9 — restart-safe reconciliation vs exchange truth.

MONEY CODE (TDD mandatory). Reconcile vs exchange every cycle and on restart; recover open positions, never double-trade.

Scaffold only — no logic yet. See docs/MASTER_DRIVER.md for the authoritative spec.
"""

# TODO(P0+): implement per docs/MASTER_DRIVER.md and the relevant docs/phases/ gate.
