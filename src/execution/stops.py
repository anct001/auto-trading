"""src/execution/stops.py — §4 — exchange-side protective stops attached at fill.

MONEY CODE (TDD mandatory). OCO/bracket where supported, so a crash/disconnect/dead process never leaves a naked position.

Scaffold only — no logic yet. See docs/MASTER_DRIVER.md for the authoritative spec.
"""

# TODO(P0+): implement per docs/MASTER_DRIVER.md and the relevant docs/phases/ gate.
