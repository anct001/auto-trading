"""src/risk/engine.py — §4 — THE single gate (Invariant 3). validate(order) -> pass/reject.

MONEY CODE (TDD mandatory). Every order — strategy, LLM-haircut, or manual UI — passes validate() before execution. No backdoor. Runtime controls may only tighten or halt, never loosen.

Scaffold only — no logic yet. See docs/MASTER_DRIVER.md for the authoritative spec.
"""

# TODO(P0+): implement per docs/MASTER_DRIVER.md and the relevant docs/phases/ gate.
