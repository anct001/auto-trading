"""src/strategy/shadow.py — §15 — shadow-mode harness (hypothetical P&L, never trades).

Computes the orders a strategy WOULD place and hypothetical P&L without trading. Caveat: shadow P&L is systematically optimistic (assumes fills at mark, ignores own impact) — a relative screen, not absolute P&L (§2).

Scaffold only — no logic yet. See docs/MASTER_DRIVER.md for the authoritative spec.
"""

# TODO(P0+): implement per docs/MASTER_DRIVER.md and the relevant docs/phases/ gate.
