"""src/strategy/ema_cross.py — §5 — the deliberately dumb first strategy (EMA crossover).

1-2 params; its only job is to exercise the pipeline end-to-end. If it looks profitable, suspect the harness before believing in edge.

Scaffold only — no logic yet. See docs/MASTER_DRIVER.md for the authoritative spec.
"""

# TODO(P0+): implement per docs/MASTER_DRIVER.md and the relevant docs/phases/ gate.
