# P3 — Monitor / orchestrate (paper) — the last paper gate

**Capital:** paper. **LLM role:** monitor / orchestrate within deterministic guardrails.

## Goal

Run the full system continuously in dry-run long enough to trust its signal behavior and
operational stability. **This is the gate immediately before real capital (P4).**

## Scope

- Shadow mode (§15): a new/modified strategy runs in shadow (hypothetical P&L, never trades)
  before promotion — a relative screen, systematically optimistic on fills (§2).
- Operator dashboard + decision log + circuit-breaker status live (§12).
- Deterministic guardrails; the LLM monitors/orchestrates but never gates a trade (Inv. 2).

## DONE-GATE

- [ ] P0–P2 gates still hold (re-proven from clean).
- [ ] **≥ N_days (default 30) continuous dry-run, no crash.**
- [ ] **Dry-run signal metrics ≈ backtest.**
- [ ] Restart-safety verified: kill mid-trade → clean recovery, no double-trade (§9).
- [ ] **Note:** dry-run does NOT validate fill realism (§2) — that waits for P4.

Passing P3 unlocks the §14 go-live checklist. Real capital is touched only after that checklist
passes with recorded human sign-off (Inv. 4, 8).

## Status (2026-06-27) — deterministic cores BUILT & tested; transport + ≥30d run remain

Built and tested (no web deps):
- **Shadow mode** `src/strategy/shadow.py` (`ShadowBook`) — hypothetical P&L from a strategy's
  intents, never trades; long-or-flat; fills at mark (optimistic, §2) → a relative promotion screen.
- **Manual-order risk preview** `src/ui/preview.py` — runs the *exact* `risk.engine.validate`
  (Invariant 9, no UI backdoor); verdict equals the engine, bad input fails closed; plus exposure
  / daily-headroom metrics for the §12 pass/fail badges.
- **Dashboard read model** `src/ui/dashboard/model.py` (`build_dashboard`) — read-only §12 view:
  equity, P&L vs soft/hard daily limits, drawdown vs kill-switch, gross + per-asset exposure,
  open positions w/ unrealized, kill-switch status, slow-loop freshness, decision-log "why".

**Remaining:**
- **Delivery (UI):** the FastAPI + SSE transport (`src/ui/api.py`) wrapping the read model +
  preview + kill-switch control, and the operator-facing market charts/heatmap/screener pixels
  (§12). These are presentation over the tested cores; they need a web stack.
- **Operational (the gate itself):** a **≥30-day continuous dry-run** on bitbank BTC/JPY with no
  crash; dry-run signal metrics ≈ backtest (`scripts/dryrun_parity.py`); and **restart-safety**
  verified (kill mid-trade → clean reconcile, no double-trade, §9). Fill realism still waits for P4.
