{
  "_comment": "Declarative params for the deliberately-dumb first strategy (§5). HASH-LOCKED by core/config (§15). Strict parameter budget (1-2 params) — this strategy's only job is to exercise the pipeline end-to-end. If it looks profitable, suspect the harness before believing in edge. Pairs/timeframe/fee tier are confirmed with the operator at ORIENT (§7) before backtest.",
  "schema_version": 1,
  "name": "ema_cross",
  "params": {
    "ema_fast": 12,
    "ema_slow": 26
  },
  "timeframe": "1h",
  "target_regime": "trend",
  "trading_venue": "binance_japan",
  "_venue_note": "Trading venue = Binance Japan only (ADR 0002, §10/§11). Pairs must be JFSA-listed on the Japan entity. Binance Global is viewing-only and never feeds this strategy.",
  "pairs": ["BTC/USDT"],
  "_pairs_note": "Chosen at ORIENT: BTC/USDT, 1h. StaticPairlist for reproducibility (§8.8). UNVERIFIED dependency: confirm BTC/USDT is listed on Binance Japan when wiring ccxt (§10) — Japan's primary quote is JPY; if absent, fall back to BTC/JPY. Fee tier still pending (needed for the backtest slice, §8.3)."
}
